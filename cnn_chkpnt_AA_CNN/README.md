CNN weights for translating from 3Di to AA.

Used in [this script](https://github.com/mheinzinger/ProstT5/blob/main/scripts/predict_AA_encoderOnly.py) to predict AA directly from 3Di using only the ProstT5-encoder (without running the decoder which is inherently slow due to its sequential decoding).
